# Next additions

## Shipped
- [x] Site systems, compounds, desk, Resend/Stripe stubs, shop images, OG
- [x] KV namespaces created + bound (`SUBSCRIBERS`, `NEWS_DRAFTS`)
- [x] `NEWS_TRIGGER_SECRET` set in Cloudflare (dashboard secret)

## Still to do
1. Verify `GET /api/health` on workers.dev (`subscribers` / `newsDrafts` / `staffSecret` true)
2. Optional: Turnstile / Stripe / Resend secrets when ready
3. Buy + attach custom domain when ready
4. Seat clinical reviewer when ready (`/authors`)
5. Replace AI catalog placeholders with real pack photography
